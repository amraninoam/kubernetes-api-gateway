apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: nginx-http-route
  labels:
    app: nginx
spec:
  parentRefs:
  - name: shared-gateway
    namespace: admin
  rules:
  - backendRefs:
    - name: nginx
      port: 80
